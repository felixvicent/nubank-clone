import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    container: {
        alignItems: 'center',
        paddingTop: 20,
        paddingBottom: 20
    },
    top: {
        flexDirection: 'row',
        alignItems: 'center',
        marginBottom: 10
    },

    title: {
        fontSize: 18,
        color: '#FFF',
        fontWeight: 'bold',
        marginLeft: 8
    }
});